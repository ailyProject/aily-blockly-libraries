# Arduino WiFiS3 库

Arduino UNO R4 WiFi 内置的 WiFi 连接库，提供对 WiFi 网络的控制和通信功能。

## 功能概览

本 Blockly 库将 WiFiS3 的核心功能转化为易用的图形化块，包括：

- **WiFi 连接**: 支持连接 WPA/WPA2 加密网络或开放网络
- **热点模式**: 创建 WiFi 接入点，使其他设备连接
- **网络查询**: 获取设备 IP、MAC 地址、DNS 等网络信息
- **网络扫描**: 扫描周围可用的 WiFi 网络
- **网络诊断**: PING 功能和固件版本查询

## 快速开始

### 基础 WiFi 连接
1. 拖入 **连接 WiFi SSID 密码** 块
2. 设置 SSID 和密码
3. 拖入 **等待 WiFi 连接** 块
4. 设置等待超时时间

### 检查连接状态
- 使用 **WiFi 已连接？** 块进行布尔检查
- 使用 **获取本地 IP 地址** 块获取设备 IP
- 使用 **WiFi 连接状态** 块获取数值状态（3=已连接）

### 扫描周围网络
1. 拖入 **扫描 WiFi 网络** 块
2. 使用 **获取扫描到的网络数量** 块获取数量
3. 用循环遍历，使用 **获取第 N 个网络 SSID** 获取网络名称

## 块分类

### 🔌 连接分类
- **连接 WiFi SSID 密码**: 连接到 WPA/WPA2 网络
- **连接 WiFi SSID（开放网络）**: 连接到无密码网络
- **启动 WiFi 热点**: 创建 WiFi 接入点
- **断开 WiFi 连接**: 断开当前连接
- **等待 WiFi 连接**: 阻塞等待连接成功或超时

### 📊 状态查询分类
- **WiFi 连接状态**: 返回连接状态码
- **WiFi 已连接？**: 返回布尔值
- **获取本地 IP 地址**: 获取设备 IP
- **获取 MAC 地址**: 获取硬件地址
- **获取网关 IP 地址**: 获取网关地址
- **获取子网掩码**: 获取子网掩码
- **获取 DNS 服务器**: 获取 DNS 地址
- **获取 WiFi 固件版本**: 获取固件版本号

### ⚙️ 配置分类
- **配置静态 IP**: 设置静态 IP、网关、子网掩码
- **设置 DNS 服务器**: 配置 DNS 解析服务器
- **设置主机名**: 设置 DHCP 主机名

### 🔍 网络扫描分类
- **扫描 WiFi 网络**: 扫描周围可用网络
- **获取扫描到的网络数量**: 返回扫描结果数量
- **获取第 N 个网络 SSID**: 按索引获取网络名称
- **获取第 N 个网络信号强度**: 获取 RSSI（信号强度，dBm）

### 🌐 网络操作分类
- **PING**: 对指定主机执行 PING，检查网络连接性

## 常见应用场景

### 1. 自动连接 WiFi 并显示 IP
```
setup时：
  连接 WiFi SSID "MyNetwork" 密码 "12345"
  等待 WiFi 连接 30000ms
  串口输出 (获取本地 IP 地址) 并换行
```

### 2. 创建 WiFi 热点
```
setup时：
  启动 WiFi 热点 SSID "MyDevice" 密码 "12345" 频道 1
```

### 3. 循环检查连接状态
```
loop时：
  如果 (WiFi 已连接?) 那么
    ...执行任务...
  否则
    ...处理断开连接...
```

### 4. 扫描并显示所有可用网络
```
setup时：
  扫描 WiFi 网络
  重复 (获取扫描到的网络数量) 次
    循环变量 i
    串口输出 (获取第 i 个网络 SSID) 并换行
```

## 注意事项

- **连接需要时间**: WiFi 连接不是瞬间完成的，建议使用 **等待 WiFi 连接** 块或循环等待
- **IP 地址格式**: 所有 IP 地址都以字符串格式返回（如 "192.168.1.100"）
- **MAC 地址格式**: MAC 地址以标准格式返回（如 "AA:BB:CC:DD:EE:FF"）
- **网络扫描**: 需要先调用 **扫描 WiFi 网络**，再通过索引查询结果
- **热点密码**: 建议设置超过 8 个字符的密码，确保网络安全

## 兼容性

- **硬件**: Arduino UNO R4 WiFi（仅支持此硬件）
- **库版本**: Arduino WiFiS3 >= 0.5.0
- **协议**: WiFi 802.11 b/g/n

## 参考资源

- [Arduino UNO R4 WiFi 官方文档](https://docs.arduino.cc/tutorials/uno-r4-wifi/)
- [WiFiS3 库源码](https://github.com/arduino/ArduinoCore-renesas)

---

**最后更新**: 2026年1月
